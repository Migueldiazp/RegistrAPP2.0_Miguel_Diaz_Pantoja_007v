import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RegistrAPP2.0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
